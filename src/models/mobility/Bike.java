package models.mobility;

public class Bike extends Vehicle {
    public Bike(String color, String registrationNumber, VehicleType vehicleType) {
        super(color, registrationNumber, vehicleType);
    }
}
