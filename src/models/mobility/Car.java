package models.mobility;

public class Car extends Vehicle {
    public Car(String color, String registrationNumber, VehicleType vehicleType) {
        super(color, registrationNumber, vehicleType);
    }
}
